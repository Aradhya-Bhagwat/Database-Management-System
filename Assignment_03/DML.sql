DROP DATABASE IF EXISTS COMPANY;
CREATE DATABASE COMPANY;
USE COMPANY;

CREATE TABLE IF NOT EXISTS DEPT (
    DEPTNO INT PRIMARY KEY,
    DNAME VARCHAR(50),
    LOC VARCHAR(50)
);

CREATE TABLE IF NOT EXISTS EMP (
    EMPNO INT PRIMARY KEY,
    ENAME VARCHAR(50),
    JOB CHAR(20),
    MGR INT NULL,
    HIREDATE DATE,
    SAL DECIMAL(10, 2),
    COMM DECIMAL(10, 2),
    DEPTNO INT,
    FOREIGN KEY (DEPTNO) REFERENCES DEPT(DEPTNO),
    FOREIGN KEY (MGR) REFERENCES EMP(EMPNO) ON DELETE SET NULL
);

INSERT INTO DEPT (DEPTNO, DNAME, LOC) VALUES
(10, 'ACCOUNTING', 'NEWYORK'),
(20, 'RESEARCH', 'DALLAS'),
(30, 'SALES', 'CHICAGO'),
(40, 'OPERATIONS', 'BOSTON');

INSERT INTO EMP (EMPNO, ENAME, JOB, MGR, HIREDATE, SAL, COMM, DEPTNO) VALUES
(7839, 'KING', 'PRESIDENT', NULL, '1981-11-17', 5000.00, NULL, 10),
(7566, 'JOHN', 'MANAGER', 7839, '1981-04-02', 2975.00, NULL, 20),
(7698, 'SAM', 'MANAGER', 7839, '1981-05-01', 2850.00, NULL, 30),
(7782, 'ALOK', 'MANAGER', 7839, '1981-06-09', 2450.00, NULL, 10),
(7369, 'SMITH', 'CLERK', 7566, '1980-12-17', 800.00, 300.00, 20),
(7499, 'ALLEN', 'SALESMAN', 7698, '1981-02-20', 1600.00, 300.00, 30),
(7521, 'ASH', 'SALESMAN', 7698, '1981-02-22', 1250.00, 500.00, 30);

SELECT * FROM EMP;

SELECT COUNT(*) AS num_employees, AVG(SAL) AS avg_salary
FROM EMP
WHERE DEPTNO = 20;

SELECT ENAME, SAL, SAL * 0.10 AS PF FROM EMP;

SELECT * FROM EMP ORDER BY SAL ASC;

SELECT ENAME, HIREDATE FROM EMP ORDER BY HIREDATE DESC;

SELECT ENAME, SAL, SAL * 0.10 AS PF, SAL * 0.50 AS HRA, SAL * 0.30 AS DA, 
       SAL + (SAL * 0.50) + (SAL * 0.30) AS gross
FROM EMP
ORDER BY gross ASC;

SELECT DEPTNO, COUNT(*) AS num_employees FROM EMP GROUP BY DEPTNO;

UPDATE EMP SET SAL = SAL + (SAL * 0.10) WHERE JOB = 'SALESMAN';

SELECT SUM(SAL) AS total_salary, MAX(SAL) AS max_salary, MIN(SAL) AS min_salary, AVG(SAL) AS avg_salary
FROM EMP
WHERE DEPTNO = 20;

SELECT ENAME FROM EMP WHERE ENAME LIKE '__I%';

SELECT MAX(SAL) AS max_salary FROM EMP WHERE JOB = 'SALESMAN';

UPDATE EMP SET SAL = SAL + (SAL * 0.10) WHERE JOB = 'SALESMAN';

DROP DATABASE IF EXISTS COMPANY;
